import 'package:flutter/material.dart';

class activityscreen extends StatelessWidget {
  const activityscreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('activityscreen'),
      ),
    );
  }
}
